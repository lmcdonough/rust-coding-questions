// Solutions are written LeetCode-style with private types, so silence the
// dead-code lint at the crate root — the tests inside each module exercise them.
#![allow(dead_code)]

// One module per coding question. Add a new `mod <name>;` line as you add files.
mod lru_cache;
